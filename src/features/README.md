# Feature module scope

Implement time-based lags and rolling statistics using timestamps rather than assumed row spacing. Every feature must be available at prediction time. Occupancy feature groups must be switchable for paired ablation.

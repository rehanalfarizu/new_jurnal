# Forecasting module scope

Implement the 30-minute persistence baseline first, then comparable historical-power and environmental models. Use temporal evaluation only. Model artifacts and fit metadata are generated outputs, not source files.
